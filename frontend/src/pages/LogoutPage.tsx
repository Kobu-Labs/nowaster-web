import { FC } from "react";
import Logout from "../components/Logout";

export const LogoutPage: FC = () => {
  return (
    <div>
      <p> LogoutPage </p>
      <Logout></Logout>
    </div>
  );
};
