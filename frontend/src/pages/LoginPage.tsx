import { FC } from "react";
import LoginForm from "../components/LoginForm";

export const LoginPage: FC = () => {
  return (
    <div>
      <p> LoginPage </p>
      <LoginForm></LoginForm>
    </div>
  );
};
