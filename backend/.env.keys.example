# =============================================================================
# JWT KEYS - SEPARATE FILE REQUIRED
# =============================================================================
# This file is separate from .env because the envy crate (used for parsing
# typed config structs) does not support multiline environment variables.
# JWT keys are accessed directly via std::env::var() instead.
#
# To use:
# 1. Copy this file to .env.keys
# 2. Replace the placeholder values with your actual RSA keys
#
# Generate new keys with:
# openssl genrsa -out private.pem 2048
# openssl rsa -in private.pem -pubout -out public.pem
#
# IMPORTANT: Keys must be on a single line with \n for newlines (as shown below)

JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_content_here\n-----END PRIVATE KEY-----"

JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\nyour_public_key_content_here\n-----END PUBLIC KEY-----"
